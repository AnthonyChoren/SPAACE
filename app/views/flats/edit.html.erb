<div class="container">
  <div class="row">
    <div class="col-sm-8 offset-sm-2">
      <%= simple_form_for [@flat] do |f| %>
      <%= f.association :planet  %>
      <%= f.input :name %>
      <%= f.input :capacity %>
      <%= f.input :description%>
      <%= f.input :price %>
      <%= f.input :photo, as: :file %>
      <%= f.submit "Update the flat", class: "btn btn-dark" %>
      <%= link_to "Cancel", flats_path, class: "btn btn-dark" %>
      <% end %>
    </div>
  </div>
</div>

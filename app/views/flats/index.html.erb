<ul>
  <% @flats.each do |flat| %>
    <li><%= flat[:name] %> <%= flat[:planet] %></li>
    <li><%= flat[:description] %> </li>
    <li><%= flat[:rating] %> </li>
  <% end %>
</ul>

<%= link_to "Add a flat", new_flat_path  %>
